- html_title @article.html_title

%h1= @article.title
%div=l @article.date, format: :default
= image_tag @article.image.thumb('300x300#').url if @article.image_uid
%div
  %strong= @article.short_text
%div= raw @article.text
