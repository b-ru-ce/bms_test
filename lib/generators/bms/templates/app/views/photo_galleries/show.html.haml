- @gallery.photos.each do |photo|
  = link_to photo.image.url + '/' + photo.image.name, title: photo.description, class: 'gallery', rel: 'gallery_js' do
    = image_tag photo.image.thumb('100x100').url